<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ggm.goguma.mapper.ProductMapper">

	<!-- 상품 목록 -->
	<select id="getProductList" parameterType="int" resultType="com.ggm.goguma.dto.ProductDTO">
		SELECT DISTINCT A.categoryid, A.productname, A.price, A.prodimgurl
		FROM (SELECT *
				FROM product p, productimage i
				WHERE p.productid = i.productid AND p.parentpid is null) A, category B
		WHERE B.categoryid = A.categoryid AND A.categoryid = ${categoryID}
	</select>
	
	<!-- 상품 개수 -->
	<select id="getProductCount" parameterType="int" resultType="long">
		SELECT count(*) AS cnt
		FROM product
		WHERE categoryid = ${categoryID}
	</select>
	
</mapper>